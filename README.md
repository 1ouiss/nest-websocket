# API

```bash
    cd api && make up
    // ou
    cd api && docker-compose -f docker-compose-dev.yml up -d

    yarn

    yarn start:dev
```

# APP

```bash
    cd app && yarn
    // ou
    yarn dev
```
